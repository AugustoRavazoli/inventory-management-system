app:
  user:
    username: admin
    password: admin

spring:
  jpa:
    hibernate.ddl-auto: none
  sql:
    init:
      mode: always
      schema-locations: classpath:database/schema.sql

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE